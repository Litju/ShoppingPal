{
  "name": "@shoppingpal/contracts",
  "version": "0.1.0",
  "private": true,
  "description": "Shared ShoppingPal contracts: money math, cart DTOs, deterministic ids. Pure TypeScript, zero runtime dependencies.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "echo \"covered by consuming workspaces\" && exit 0"
  },
  "devDependencies": {
    "@shoppingpal/config": "workspace:*",
    "typescript": "~5.9.3"
  }
}
